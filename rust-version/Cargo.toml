[package]
name = "hegel-c"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["staticlib"]

[dependencies]
hegeltest = "0.4"
libc = "0.2"

[build-dependencies]
cc = "1.0"

[profile.release]
panic = "unwind"
